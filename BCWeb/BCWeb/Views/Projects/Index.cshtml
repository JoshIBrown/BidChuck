@{
    ViewBag.Title = "Projects";
}

<div class="row">
    <div class="large-4 columns left">
        <h2>Projects</h2>
        @if (User.IsInRole("architect"))
        {
            @Html.RouteLink("Create new Project", "Default", new { controller = "Projects", action = "Create" })
        }
    </div>
</div>
